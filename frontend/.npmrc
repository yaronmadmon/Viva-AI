# Suppress EBADENGINE warnings when Node is older than some devDependencies expect.
# Next.js runs on Node 18+. For best compatibility with all tools (Vite, Playwright, etc.), use Node 20.19+ or 22 LTS.
ignore-engines=true
